# Local dual-port (optional): Nest HTTP on 3010, raw WebSocket on 3001.
# On Railway/Render/Fly: do NOT set HTTP_PORT — only the platform PORT is used
# (HTTP /rooms and WebSocket share the same port).
PORT=3001
HTTP_PORT=3010
HOST=0.0.0.0
